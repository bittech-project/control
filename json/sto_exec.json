{
  "subsystems": [
    {
      "subsystem": "sto_control",
      "config": [
        {
          "method": "exec",
          "params": {
            "cmd": ["ls", "-h"],
            "capture_output": true
          }
        },
        {
          "method": "exec",
          "params": {
            "cmd": ["echo", "Dimec and Gleban"],
            "capture_output": false
          }
        },
        {
          "method": "exec",
          "params": {
            "cmd": ["ls", "-h"],
            "capture_output": false
          }
        },
        {
          "method": "exec",
          "params": {
            "cmd": ["echo", "Dimec and Gleban"],
            "capture_output": true
          }
        }
      ]
    }
  ]
}
