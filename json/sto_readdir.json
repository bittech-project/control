{
  "subsystems": [
    {
      "subsystem": "sto_control",
      "config": [
        {
          "method": "control",
          "params": {
            "subsystem": "sys",
            "op": "readdir",
            "dirpath": "/root/gleb/control"
          }
        }
      ]
    }
  ]
}
