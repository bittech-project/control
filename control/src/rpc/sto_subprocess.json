{
  "subsystems": [
    {
      "subsystem": "sto_control",
      "config": [
        {
          "method": "subprocess",
          "params": {
            "cmd": ["ls", "-h"],
            "capture_output": true
          }
        },
        {
          "method": "subprocess",
          "params": {
            "cmd": ["echo", "Dimec and Gleban"],
            "capture_output": false
          }
        },
        {
          "method": "subprocess",
          "params": {
            "cmd": ["ls", "-h"],
            "capture_output": false
          }
        },
        {
          "method": "subprocess",
          "params": {
            "cmd": ["echo", "Dimec and Gleban"],
            "capture_output": true
          }
        }
      ]
    }
  ]
}
